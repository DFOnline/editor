{
  "name": "dfonline",
  "version": "0.0.10",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "parcel": "^2.0.1",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "@types/node": "^16.11.11",
    "@types/pako": "^1.0.2",
    "nbt-ts": "^1.3.4",
    "ts-node": "^10.4.0"
  },
  "scripts": {
    "test": "npx parcel \"src/**/*.html\" --no-cache --dist-dir ./docs --lazy",
    "build": "npx parcel build \"src/**/*.html\" --no-source-maps"
  }
}
