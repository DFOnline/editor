{
  "name": "dfonline",
  "version": "0.0.4",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "parcel": "^2.0.1",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "@types/node": "^16.11.11",
    "@types/pako": "^1.0.2",
    "ts-node": "^10.4.0"
  },
  "scripts": {
    "test": "powershell parcel 'src/**/*.html' --no-cache --dist-dir ./docs",
    "build": "powershell parcel build 'src/**/*.html' --dist-dir ./docs;"
  }
}
